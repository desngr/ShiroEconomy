debug: false
locale: "" # Example: "en" - English

# By default plugin is using SQLite database
mysql:
  enabled: false
  url: ""
  user: ""
  password: ""

# Optional. LuckPerms is required too
huge-transaction:
  enabled: true
  minimum: 250000 # Minimum amount for huge transaction announcement
  groups: # LuckPerms groups
    - "personnel"
    - "staff"